services:
  snap:
    image: ghcr.io/dorksoul/snap:latest
    ports:
      - "3000:3000"
    volumes:
      - /path/to/your/media:/media:ro
    environment:
      - MEDIA_ROOT=/media
      - PORT=3000
    restart: unless-stopped
